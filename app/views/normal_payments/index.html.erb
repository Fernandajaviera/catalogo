<p id="notice"><%= notice %></p>

<h1>Normal Payments</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @normal_payments.each do |normal_payment| %>
      <tr>
        <td><%= link_to 'Show', normal_payment %></td>
        <td><%= link_to 'Edit', edit_normal_payment_path(normal_payment) %></td>
        <td><%= link_to 'Destroy', normal_payment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Normal Payment', new_normal_payment_path %>
